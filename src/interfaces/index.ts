export interface Button {
    id: string, 
    text: string
};

export interface Infomation {
    body: string,
    title: string, 
    hour: string,
    userAdmin: string
}

export interface UserInformation {
    name: string,
    status: number,
    number: string
}